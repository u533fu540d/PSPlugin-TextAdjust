# 批量调整工具 · 使用说明

一个 Photoshop **CEP 扩展**（HTML/JavaScript 面板），用于按「字符属性」批量筛选并修改文本图层，也可按尺寸/主色调筛选图片图层并批量缩放。

CEP 面板是**非模态**的：面板打开时，可以同时操作 Photoshop（切换工具、点选图层、编辑画面等）。

---

## 一、安装

### 一键安装（推荐）

双击目录下的 **`安装插件.bat`**。安装程序会自动完成：

- **索引本机环境**：列出检测到的 Photoshop 版本与所有 CEP 扩展目录；
- **解决签名问题**：为 `HKCU\Software\Adobe\CSXS.5` ~ `CSXS.25` 写入字符串 `PlayerDebugMode=1`
  （管理员运行时会同时写入 `HKLM`），允许加载未签名扩展；
- **安装插件**：在 `%APPDATA%\Adobe\CEP\extensions\plugin-cep` 创建指向仓库内 `plugin-cep\` 的
  **目录联接（Junction）**，源码改动即时生效、无需重复复制；
- **加固调试标记**：在扩展目录放置 `.debug` 文件，强制以调试模式加载，进一步绕过签名校验。

安装完成后：

1. **完全退出并重启 Photoshop**；
2. 菜单 `窗口 > 扩展(旧版) > 批量调整工具`。

> **卸载**：双击目录下的 `卸载插件.bat`。
>
> 命令行用法：
> ```
> powershell -ExecutionPolicy Bypass -File tools\install-cep.ps1
> powershell -ExecutionPolicy Bypass -File tools\install-cep.ps1 -AllUsers        # 安装到所有用户（需管理员）
> powershell -ExecutionPolicy Bypass -File tools\install-cep.ps1 -Copy            # 复制代替联接
> powershell -ExecutionPolicy Bypass -File tools\install-cep.ps1 -Uninstall
> powershell -ExecutionPolicy Bypass -File tools\install-cep.ps1 -Uninstall -RemoveDebugMode
> ```

### 手动安装

1. 把 `plugin-cep\` 复制到 CEP 扩展目录（二选一）：
   - 当前用户：`%APPDATA%\Adobe\CEP\extensions\`
   - 所有用户：`C:\Program Files (x86)\Common Files\Adobe\CEP\extensions\`
2. 解决签名：注册表 `HKEY_CURRENT_USER\Software\Adobe\CSXS.12` 新建**字符串** `PlayerDebugMode` = `1`
   （PS 2024/2025/2026 用 `.12`；不确定就把 `.9`~`.25` 都建一份）；
   也可在扩展目录内新建一个内容为空的 `.debug` 文件。
3. 重启 Photoshop → `窗口 > 扩展(旧版)` → 打开「批量调整工具」。

---

## 二、界面结构

顶部两个 Tab 分为：**文本批量调整** 与 **图片批量调整**。文本模块自下而上分四个区域：**筛选区 → 图层列表 → 批量修改 → 底部操作栏**。

图片模块包含：图片筛选区、图片列表、图片缩放区与底部应用按钮。目前图片模块先实现缩放功能。

### 1. 顶部

- **扫描 / 刷新**：重新读取当前文档的文本图层。打开/切换文档后会自动刷新。
- 下方显示当前文档名。

### 2. 筛选区（改动即时刷新列表）

| 项 | 说明 |
| --- | --- |
| 关键字 | 匹配图层名称与文字内容 |
| 字体 | `包含 / 排除` + 点「选择」展开具体字体字形多选；每行显示“字体系列 - 字体样式”，同系列不同样式分行显示，支持按住拖选/拖取消；缺失字体以灰色标注“(字体缺失)” |
| 颜色 | `不限 / 匹配 / 排除` + 色块（点击打开选色弹层） |
| 容差 | 滑块 + 数值框（0–100%），用于匹配“相近色” |
| 字号 | 最小 ~ 最大（留空表示不限） |
| 粗体 / 斜体 / 可见 | `不限 / 是 / 否` |
| 重置全部筛选 | 一键清空上面的全部筛选条件 |

**颜色筛选规则**：按 RGB 每通道差值比较，容差 0 表示精确相等；容差越大，接受的颜色越接近。例如容差 10 ≈ 每通道允许约 25 的偏差。

### 3. 图层列表

- **默认全选**当前文档中的所有文本图层。
- **单击整行**即选中 / 取消（选中行显示灰蓝色背景），无需复选框。
- **按住鼠标拖动**可连续选择多行：从未选中的行开始拖动会一路选中，从已选中的行开始拖动则一路取消。
- 拖动工具栏中的「行高」滑杆可调整列表行尺寸；行高降到 40 或以下时会自动隐藏正文预览，只显示图层标题与右侧属性。设置会自动记忆。
- 工具栏：`全选 / 反选 / 清空 / 选中`，左侧显示 `匹配 N / 共 M，已选 X`。
- 点「选中」可将当前列表内的灰蓝色选中项同步选入 Photoshop 图层面板，便于直接移动、变换或执行其它 Photoshop 操作。
- 每行右侧显示该图层的属性：**色块 · 字体系列 - 字体样式 · 字号 · 粗/斜标签**（标签高亮表示该属性开启）。

### 4. 批量修改（对“选中”的图层生效）

| 行 | 项目 |
| --- | --- |
| 1 | 字体系列 + 样式（系列下拉含「（不修改）」；选定系列后再选择常规、粗体、斜体等具体样式） |
| 2 | 字号（不修改 / 固定值 / 按比例） · 颜色（不修改 / 设为 + 色块 + HEX） |
| 3 | 粗体 · 斜体 · 下划线 · 删除线 |
| 4 | 大小写（正常 / 全大写 / 小型大写） · 基线（正常 / 下标 / 上标） · 偏移（基线偏移 px） · 内容（大写 / 小写 / 标题，直接改写文字内容） |
| 5 | 字距 · 行距 · 水平% · 垂直% |
| 6 | 消除锯齿 · 自动字距（度量标准 / 视觉 / 手动） |

- 点右上角「收起 / 展开」可折叠该区域，给列表让出空间。
- 只有设置了内容的项才会被应用；未改动的项保持原样。

### 5. 底部操作栏（常驻）

- 左侧显示 `已选中 N 个图层`；右侧 **撤销 / 应用到选中项** 按钮始终固定在窗口底部。
- **撤销** 会调用 Photoshop 撤销上一轮批量修改；新的批量应用会尽量作为一个历史步骤写入。
- 未选中任何图层时点应用，会提示先去列表中选择。
- 下方「调试输出」可展开日志，查看每次应用的过程、成功/失败与错误信息。

### 6. 图片批量调整

| 项 | 说明 |
| --- | --- |
| 筛选：关键字 | 匹配图片图层名称 |
| 筛选：宽度/高度 | 输入最小值与最大值，单位 px，留空表示不限 |
| 筛选：主色 | 扫描/刷新不会自动提取主色。切换为匹配/排除或点击「提取主色」时会弹出确认；确认后插件把单个图层复制到临时小文档，并从 RGB 通道直方图计算平均主色，不修改原文档 |
| 图片列表 | 与文本列表一致，支持行高调整、全选、反选、清空、拖选、同步选中到 Photoshop |
| 缩放：等比缩放到尺寸 | 填写目标宽和/或高；同时填写时按能放入目标尺寸的比例缩放 |
| 缩放：按百分比缩放 | 直接输入缩放比例，例如 `50` 表示缩小到 50% |
| 中心点 | 九宫格选择锚点：中心、四角、四边中点 |

---

## 三、颜色选择弹层

点击筛选区的**色块**打开：

- **色环 + 内嵌方块**：拖动色环调整色相，拖动方块调整饱和度/明度；
- **吸管按钮**（左上）：点击后打开 Photoshop 原生颜色选择器，可在 Photoshop 画布上吸色；确定后回填到插件，取消或按 `Esc` 不修改；
- **预览色块**（右上）：实时显示当前颜色；
- **模式下拉**：`RGB 0-1.0` / `RGB 0-255` / `HSV` / `HEX`；
- **通道滑条**：每个通道带渐变轨道，可拖动或用数值框输入；
- `确定` 写回颜色并自动把筛选模式切到「匹配」；`取消` / 点遮罩关闭。

> 吸色说明：调用 Photoshop 原生颜色选择器，不依赖 PowerShell 或外部程序。插件会临时使用当前颜色作为初始值，取色结束后恢复 Photoshop 原前景色，避免影响当前对象。

---

## 四、应用逻辑

1. 只对列表中**已选中**的文本图层生效。
2. 逐层写入属性；应用期间临时设置 `displayDialogs = NO`，抑制「缺少字体」等弹窗，一次性完成。
3. 完成后自动重新扫描，列表与属性同步刷新。
4. 修改会进入 Photoshop 的历史记录，可用 `Ctrl+Z` 撤销。

---

## 五、目录结构

```
PSPlugin-TextAdjust/
├─ 安装插件.bat               一键安装（双击运行）
├─ 卸载插件.bat               一键卸载（双击运行）
├─ plugin-cep/                插件本体（安装到 CEP 扩展目录）
│  ├─ CSXS/manifest.xml       扩展清单（面板类型、入口、菜单名、尺寸）
│  ├─ index.html              面板界面
│  ├─ css/styles.css          样式
│  ├─ js/main.js              界面逻辑 + 与 Photoshop 通信
│  └─ jsx/host.jsx            Photoshop 宿主脚本（TA_scan / TA_getFonts / TA_apply / TA_selectLayers）
├─ tools/
│  └─ install-cep.ps1         安装/卸载逻辑（供两个 .bat 调用）
├─ 使用说明.md
└─ README.md
```

> 安装后仅生成一个指向 `plugin-cep\` 的目录联接，源码改动即时生效。`.debug` 会随安装生成在 `plugin-cep\` 内（已在 `.gitignore` 中忽略）。

`main.js` 通过 `window.__adobe_cep__.evalScript` 调用 `host.jsx` 中的函数，数据以 JSON 字符串传递。

---

## 六、常见问题

| 现象 | 处理 |
| --- | --- |
| `窗口 > 扩展(旧版)` 是灰的 | 未安装可加载的 CEP 扩展：确认目录位置、`CSXS/manifest.xml` 存在、已开启 `PlayerDebugMode`，并重启 PS |
| 提示“未经正确签署”/扩展被拒绝加载 | 重新双击 `安装插件.bat`（写入 `PlayerDebugMode` 并放置 `.debug`），然后重启 PS |
| 面板能开但列表为空 | 文档没有文本图层，或未点「扫描 / 刷新」 |
| 中文被换成其它字体 | 所选字体不含中文字形，Photoshop 会自动替换。请选含中文字形的字体 |
| 某项修改没生效 | 展开「调试输出」查看错误；下划线/删除线等依赖枚举，若失败会记录 |
| 打开时闪退 | 确认是 CEP 面板而非有问题的 ExtendScript 非模态窗口；重启 PS 后再试 |

---

## 七、注意

- 插件本体位于 `plugin-cep\`，由 `安装插件.bat` 安装到 CEP 扩展目录。
- 字体显示名取自 Photoshop 的 `TextFont.family`；若某字体自身只提供英文系列名，界面会保留该名称，实际应用始终使用 Photoshop 的具体字体 ID。
- CEP 属较旧技术，Adobe 已不再演进，但至今仍被 Photoshop 支持。
